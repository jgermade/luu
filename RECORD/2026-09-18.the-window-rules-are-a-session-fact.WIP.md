# The window rules are a session's fact, not a screen's

**Status:** WIP — a proposal, waiting on the code it plans. It answers item 6 of
[`ROADMAP/2026-09-17`](../ROADMAP/2026-09-17/README.md) (*whether any default
flips*) in a shape the row did not consider, and grows that revision a row for
the rest.

**Provenance.** A conversation, and it is recorded as one because **both halves
of the proposal moved under examination** and the movement is the useful part.
It arrived as *make the three window rules configurable from the web settings
modal, defaulting to the one that saves most.* The location was wrong against a
decision already written; the criterion was right and I had read it wrong. Both
are below, in the order they were argued, because a record that only prints the
conclusion cannot be argued with.

## The problem, in three parts

**One — three rules nobody can reach from the surface they use.** Rules A, B and
C are built, tested and off by default (`--repeat-once`, `--prune-behind`,
`--prune-results`). They are reachable by typing a flag at `luu serve` and
restarting it. That is the same sentence
[`the-gate-panel-narrows`](2026-09-17.the-gate-panel-narrows.completed.md) wrote
about `network`, `egress` and `enforcement` the day before yesterday — *a
narrowing nobody can reach from the surface they actually use is a narrowing
nobody applies* — one surface along, and this time it is not a narrowing but the
project's own differentiator.

**Two — item 6 has been undecided since 2026-09-14.**
[`the-7b-does-not-miss-it`](2026-09-14.the-7b-does-not-miss-it.completed.md)
closed the measurement and explicitly declined the decision, correctly. The
roadmap has carried it as a row ever since, with the sentence that is the whole
of why it is a row: **a flag nobody ever turns on is a feature nobody has.**

**Three, and this one was found while checking the other two, and it is a defect
rather than a gap.** `RecordLine::Header` (format 10) carries `format`,
`protocol`, `backend`, `model`, `context_limit`, `counter`, `eviction` and
`posture`. It does not carry `repeat`, `prune` or `results`. The four optional
fields it does carry are each there under the same comment, written three
separate times:

> *Two runs under different policies are not comparable, and by the time anyone
> is comparing them nobody remembers which was which.*

So **the header names the policy that decides what *leaves* the window and not
the three rules that decide what the window is *made of*.** The only thing
either rule puts on the wire is the `pruned` trace line, and it fires when rule
B prunes something — which means a `--prune-behind` run that never found a span
to prune is byte-indistinguishable from a run without the flag, and a
`--repeat-once` run is indistinguishable from `off` in every case. There is a
run on disk under each of these flags. Nothing in those files says so.

`prune-behind`'s own record already worried about exactly this class of thing —
*"a flag that quietly starts meaning something else turns a recorded arm into an
unrecorded one"* — and guarded it by making rule C a third flag rather than a
widening of the second. The guard was right and it was applied to the flag
rather than to the file.

**This third part is independent of the rest of this proposal and lands first.**
A project whose whole method is that a run can be re-read afterwards cannot make
these rules easier to turn on while the recording still cannot say they were.

## The criterion, and the correction it went through

The proposal said *default to the one that saves most*. Ranked by tokens saved,
that is unambiguous and it is rule C:

| | flag | what it frees | eviction | accuracy measured |
| --- | --- | --- | --- | --- |
| A | `--repeat-once` | `code` bucket **−17.5%**, prompt −4.4% | 12 cuts → 11 | **yes** — 20 questions, `qwen2.5-coder:7b`, verdicts identical |
| B | `--prune-behind` | prompt **−0.3%** | 13 → **0** | no |
| C | `--prune-results` | **32 752 tokens** over 20 turns | 16 → **0** | no |

C is also the rule with no accuracy measurement at all, and the one whose
failure mode is the worst of the three: it replaces tool output with a citation,
and *the output is evidence*. The closing condition is safe — it reads exit
codes off `ToolStep`, not off the prompt — but the model reasoning about a
`cargo test` it can no longer see is item 5 of the revision, open and unbought.
So *the one that saves most* selects the only one we know nothing about.

**The criterion was not that, and the difference is the whole of why this
proposal has the default it has.** Restated by its author: *the one that
consumes fewest tokens **without affecting the result**.* That is a different
function over the same table. It ranks nothing that has not cleared the second
bar, and today exactly one rule has both halves:

```
savings alone            →  C  (32 752 tokens, and no idea what it costs)
savings proven free      →  A  (−17.5% of the code bucket, 0 verdicts moved)
```

Worth writing down because the two readings differ by four words and select
different defaults, and because the second is the axis this project has used
everywhere else: a number is not a finding until it carries what it cost.

## The proposal

Four changes, in the order they should land.

**1. The header carries the three rules.** A `repeat`, a `prune` and a `results`
field beside `eviction`, `Option<_>` for the same reason and with the same
`None` semantics — a stream written before format 11 does not say, and inventing
the field on the reader's behalf would put a claim in a record the record never
made. `record::FORMAT` 10 → 11. This is the defect above and it is independent
of the other three.

**2. Per session, not per process.** Today `repeat_once`, `prune_behind` and
`prune_results` arrive at `serve` once, as process-wide parameters beside
`context_limit` and `evict`, and every session the process runs shares them.
Choosing them from a page means choosing them per session first. **That is the
real work of this proposal**, and it is not in the modal.

**3. Named in `config.toml`, chosen at session start, immovable on resume —
which is the posture's shape, deliberately.** `[posture.<name>]` names a policy
file, `serve` offers the names to the page, the session picks one when it
starts, the header records it, and a resume that disagrees is refused with a 409
because *its jobs were approved under that one*. The window rules want four of
those five properties for the same reason and the fifth for a weaker one: a
resumed session's stored turns were rendered under a policy, and
`Context::from_view` already rebuilds turns without their fragments, so a
session that changes rules on resume is a session whose recording describes two
different windows under one header.

```toml
# the machine's default, and what a session starts on unless it says otherwise
[window]
repeat  = "once"      # rule A — on, and see the default below
prune   = "never"     # rule B
results = "kept"      # rule C, inert without prune
```

**4. A third Settings section, and not General.** The modal's own comment says
the shape allows it — *a section is a name and a component, not a place in a
scroll* — and General is closed to this by a decision already in
[`luu-design.md`](../luu-design.md):

> *Preferences live in `prefs.js` and in `localStorage`: theme, which editor
> draws a file, the layout, and who answers the gate. **None of it is in
> `config.toml`, which says where a run sends — a fact about the run, not about
> the screen somebody reads from.***

General holds Theme, Editor, Layout, Files and Workspace: five facts about the
screen. The window rules are three facts about the run — they change the bytes
the model is sent. A `localStorage` key that changes the prompt would mean
clearing browser data changes what a run sends, the same session sends
differently from a second browser, and the header records a fact the page can
contradict. The section sits beside Models, which is the other thing in that
modal that is about the run rather than the reader.

**And the default: rule A on, B and C off.** Not the most savings — the most
that has been *earned*. A is the only one of the three that has been through a
model, and it moved no verdict across 20 questions while taking 17.5% out of the
`code` bucket. B and C wait on item 5. This is the decision item 6 asks for, made
on the evidence there is and stated as exactly what it is: **one conversation,
one 7B, one quantization, one window size, one seed, run once.** Turning it on
by default is a claim that that is enough to stop making everybody type it; it
is not a claim that the distance question is answered.

## What it costs

- **A format bump, 10 → 11**, and three optional fields on the header. Readers
  of a format-10 file get `None` on all three, which is the same contract
  `eviction` already has.
- **One budget policy per session where there is one per process**, which is the
  bulk of the diff and touches `serve`, the session store and the fold.
- **A refusal on resume**, and its 409, modelled on the posture's. Cheaper than
  that one was: no container can start from this decision, so the fold has
  nothing to build.
- **A behaviour change for every existing user of `serve` and `chat`**, which is
  what flipping a default *is*. `--repeat-once` becomes the default and wants a
  `--no-repeat-once` to turn off; every recorded arm that says `off` stays
  readable, and every *future* arm has to say which side it was on — which is
  change 1, and is why change 1 goes first.
- **Tests**: a header round-trip per rule, a session started under each from the
  page, a resume that disagrees, and one that pins the default so that flipping
  it again is a diff somebody reads rather than a number that drifts.

## What was rejected

- **`localStorage`, or anywhere in General.** Argued above. The shortest version
  of why: a preference that changes the prompt is not a preference.
- **Defaulting to the rule that saves most tokens.** That is C, which is inert
  without B, unmeasured for accuracy, and the one that replaces evidence with a
  citation. It is also the one item 5 exists to ask about.
- **Defaulting all three on** *because each has a number*. B's number is −0.3% of
  the prompt: what it buys is not tokens, it is that nothing is evicted — 0 turns
  dropped against 13, the whole conversation still in the window. Ranking the
  three by savings puts the most interesting one last, which is a good sign the
  ranking is the wrong instrument.
- **A named window profile** (`[window.thrifty]`, chosen by name like a posture).
  The indirection buys a short label in the header and costs a second place to
  look up what a run did. Three fields say it outright. Reconsider if a fourth
  rule ever lands.
- **Leaving it to flags and writing nothing.** That is the state item 6 has been
  in for four days, and it is the only outcome of the three the roadmap names
  that has no argument behind it.
- **Treating this as an answer to item 6.** It is not, and the row should not be
  struck through by it. Making a default reachable from three places does not
  decide which default ships; this proposal decides that separately, in one
  sentence, on one run's evidence, and says so.

## Still open

- **Item 5 is untouched and it gates B and C.** Whether a model minds losing tool
  output to a citation is the same question it was on 2026-09-14, and no part of
  this proposal moves it. B and C stay off until it is answered, whatever a page
  makes easy.
- **The distance question for rule A**, named as the real one by
  [`a-span-is-rendered-once`](2026-09-07.a-span-is-rendered-once.completed.md)
  and not stressed by `grounded.txt`, whose repeats are a handful of turns apart
  rather than eleven. Flipping the default does not answer it and a second
  corpus still should.
- **n=1 remains n=1.** If a second corpus or a second model ever contradicts the
  2026-09-14 run, the default flips back and this file gets an appended section
  saying so. That is the cost of deciding on one run, accepted knowingly here.
- **Nothing counts how often any of the three fires.** Third instance of the same
  gap in one revision — after item 12's file-versus-directory grants and the
  `not_granted` refusal nobody counts — and the same sentence applies: a rule
  that fires every turn and one that fires twice a week are different features,
  and this tree cannot tell them apart.
- **Whether `chat` should follow `serve`.** This proposal makes the rules a
  session's fact and the CLI has no session-start moment to put them in. The flag
  stays the flag there, which is a seam between the two surfaces that nobody has
  argued about yet.

---

## Appended 2026-09-18, later the same day: the sentence about "runs on disk" is wrong, and the defect is bigger than it

Checking the §Problem claim in order to explain it found it overstated. The
original sentence, left above where it was written:

> *There is a run on disk under each of these flags. Nothing in those files says
> so.*

**Both halves are wrong, in opposite directions.**

`*.jsonl` is gitignored project-wide and `git ls-files` finds zero of them, so
there are no recordings in this tree at all — only each run's `README.md`,
`stats.json` and its transcripts. And each of those READMEs *does* say which arm
it was:

```sh
for arm in "off:" "prune-behind:--prune-behind" \
           "prune-behind-and-results:--prune-behind --prune-results"; do
  target/release/luu chat --script script.txt … --record ${arm%%:*}.jsonl ${arm#*:}
done
```

So the provenance is not lost. What it is, is **beside the recording rather than
in it, in prose somebody wrote by hand.** Which is the argument for the header
rather than against it: `context_limit`, `counter`, `eviction` and `posture` are
all named in run READMEs too, and every one of them is in the header anyway,
under a comment saying why. Nobody has ever argued that the header is redundant
with the prose next to it. `2026-09-14.prune-results/README.md` says its numbers
were computed "with `jq` directly against the (uncommitted) recording" — a reader
re-deriving those numbers has the file, and the file cannot tell them which arm
they are holding.

**And the half the original sentence missed entirely, which is why this section
exists rather than a one-line erratum.** `RecordLine` is not only what `--record`
writes to a file. It is the stream the **session store** persists and the one
`SessionView` is folded from — `api.rs` mirrors the header's `posture` onto the
live view precisely so a resumed session recovers it, and that is *why* `posture`
was added to the header on 2026-09-17 in the first place, one day before this.

So a resumed session cannot know what rules its stored turns were rendered under,
because the fact was never written down anywhere the fold can reach. That
reorders this proposal's own parts: **part 3 is blocked on part 1 rather than
merely sequenced after it.** A resume cannot refuse a mismatch on three facts a
resume has no way to read. The original §Proposal put the header first for the
weaker reason that it was an independent defect; it is first because nothing else
here can be built without it.

**What this says about the original claim**, and it is the reason the sentence
stays above rather than being fixed: it was reaching for the right defect with an
example it had not checked. The example was decoration — *look, there are files
that don't say* — and the mechanism was load-bearing and unexamined. Checking
decoration is how the mechanism turned up. Fifth time in eleven days that reading
code for a reason other than finding a bug is what found one, and the first where
what nothing drove was a sentence in a record rather than a branch in the tree.

---

## Appended 2026-09-18, third section: two corrections from the proposal's author, and a fifth part

Both are corrections to §Proposal above, and both are right. They stay here as a
section because the parts they correct were wrong for instructive reasons: one
borrowed an argument it had not earned, and the other stopped one level too
coarse.

### 1. Written to `config.toml` from the page, and applied to the live session

The location half of §Proposal stands and this sharpens it: the value is written
back to `config.toml` when it is saved, and takes effect on the session already
running.

**"Immovable on resume" is withdrawn.** It was copied off the posture together
with the posture's restriction, and only the shape was earned. A posture may not
move because *its jobs were approved under it* — an approval argument, about a
grant outliving the thing that bounded it, argued in
[`what-an-approval-was-granted-under`](2026-09-17.what-an-approval-was-granted-under.completed.md).
The window rules grant nothing and bound nothing. Nobody approves a job under
`--repeat-once`. Reasoning by analogy from the posture imported a refusal that
has no argument behind it here, which is the failure
[`AGENTS.md`](../AGENTS.md) names about routing around a recorded decision, one
step removed: this routed *through* one.

**The mechanism for changing it live already exists and §Proposal did not
mention it.** `retarget_header` compares the live session against where it is
sending and, when backend, model or posture have moved, writes **a fresh header
line into the middle of the stream**. It gained its third term the day before
this record was written. The window rules are its fourth, and they are the only
one of the four that is already sitting in the argument it is handed:
`session::header` takes a whole `Budget` and writes down `budget.limit` and
`budget.eviction` while `budget.repeat`, `budget.prune` and `budget.results` are
in the same struct, in its hand, unread.

So §Problem part three is smaller than it looked and worse than it read. The
fact is not hard to reach. **The function that had all five fields wrote down
two.** And the two doc comments on the builders say why it should have written
five — *"a recording made under it is not the same arm as one made without it"* —
so the code states the reason for the field in a comment beside the call that
drops it.

**The write path exists too.** `PUT /api/providers` already writes `config.toml`
from the browser: refused with a 403 on a server bound off loopback, and rebuilt
from the file on disk first, under a comment saying the page writes one table and
must not delete the other. A `[window]` table is a sibling of that route under
the same two guards.

**What it costs, and it is the one part of this that is not already
precedented: the toggle is not symmetric.** The prune line is a ratchet — *"the
line is a ratchet, so what it gives up now it gives up for good"* — so turning
rule B **on** mid-session is clean (the line starts moving) and turning it
**off** does not give back what was already pruned, because the line does not
move backwards. Rule A has no such state: its owner is chosen inside the window
being rendered and never over the session, so A is clean in both directions.
Three ways out, none chosen here: say so at the control, make the ratchet
conditional on the rule still being on, or refuse the off-switch mid-session and
keep it to a session start. The first is honest and cheap, the second is a change
to a mechanism two records argued for, the third is the restriction this section
just withdrew, reappearing with an argument this time.

### 2. Tool output is not one kind of thing, and `Results` treats it as one

Half of this is already true and is why rule C is a separate flag from rule B:
quoted file content and tool output are different objects, argued in
[`what-a-result-costs`](2026-09-09.what-a-result-costs.completed.md). The
correction goes one level further, and it is right: **`Results` is binary over
*every* result**, `Kept` or `Cited`, and the five tools in the registry are not
one kind of thing.

| tool | what its output is | losing it to a citation |
| --- | --- | --- |
| `list_dir` | a view of the tree, re-derivable | cheap |
| `read_file` | file bytes — *the same object as a selected span*, by this project's own account | cheap |
| `run_command` | **evidence**: exit codes, test output, the thing a closing condition reads | the whole of item 5's worry |
| `write_file`, `edit_file` | a confirmation, already small | nothing to save |

It is cheap to build: `Results::Cited` decides at a point that is already holding
`ToolStep.call.name` — `result_text` renders through it.

**And it may close item 5 rather than wait for it, which is the part worth
checking before anything is built.** The 32 752 tokens rule C freed were measured
on a corpus where **every one of the 20 turns called `read_file`** on a 136 KB
file, truncated at the 8 KiB cap every time. So every measured token of rule C's
win is in the row this table calls cheap, and **none** of it is in the row item 5
is about. A per-tool rule C could ship the whole measured saving while making
none of the unmeasured claim.

Stated as what it is: **a proposal, not a measurement.** No corpus in this
repository has ever pruned a `run_command` result, so nothing here says what that
costs — only that the number rule C is currently justified by never tested it.

### What this does to the order

- **Part 1 is unchanged and still first**, for the reason the second appended
  section gave and now also because it is what makes a mid-session change legible
  at all: a rule that moves at turn 12 needs a stream that can say so.
- **Part 3 becomes live rather than immovable**: a `[window]` table, a `PUT`
  beside the providers one with the same two guards, and a header line through
  `retarget_header` when it moves.
- **Part 5, new**: `Results` becomes per-tool. It should land before anybody
  turns rule C on, because the alternative is turning on a rule whose justifying
  number came entirely from the tools it is safe on.

---

## Appended 2026-09-18, fourth section: the asymmetry, read in the code, and it is worse than the third section said

The third section named the ratchet and voted for the cheap way out. Reading the
two sites to explain it found a third rule's behaviour it had not separated, and
a consequence that changes the vote.

**Two sites, and only one of them asks whether the rule is on.** Moving the line
is gated:

```rust
if budget.prune == Prune::Behind && self.fits_from(…) > self.floor {
    self.pruned = self.prune_to(target, counter, budget.repeat, budget.results);
```

Rendering below it is not:

```rust
Item::Turn(index) if *index < self.pruned => {
```

That branch asks only whether a turn is below the stored line. So turning rule B
off mid-session stops the line advancing and returns nothing that is already
behind it, for the rest of the session. The third section's claim is confirmed,
and the mechanism is this: the policy gates the *decision* and the line gates the
*render*, and they are not the same gate.

**The three rules are not one case, which the third section ran together.** The
distinction is whether a rule is a render parameter or stored state:

| | on | off | why |
| --- | --- | --- | --- |
| A `repeat` | clean | **clean** | passed per render, stores nothing |
| B `prune` | clean | **does not undo** | the line is session state; the render obeys it ungated |
| C `results` | clean | clean *in itself* | also a parameter — but only reaches turns below B's line, left where B left it |

**And the consequence that changes the vote.** Turning any of them off makes the
prompt bigger, and the way this window absorbs bigger is the floor. The floor is
the other ratchet and it is the destructive one: what it drops takes the question
and the answer with it. So switching rule C off mid-session can hand back tool
output and spend the conversation for it — bigger prompt, over budget, floor
moves, turns gone, and the floor does not come back either.

That is a different sentence from *you do not get the pruned spans back*, and
saying it at the control is no longer obviously enough. Revised standing, still
not a decision:

- **A is safe to toggle live in both directions** and is the default this record
  proposes flipping. Nothing below applies to it.
- **B and C are safe to turn on live and are not safe to turn off live**, for two
  different reasons — B because its line will not retreat, C because what it
  gives back has to fit somewhere and the floor is what pays.
- The honest cheap option is therefore narrower than the third section's first
  way out: **live on, session-start only for off.** That is the third way out
  reappearing, and this time with the argument the section said it was missing.
  It is also the only one of the three that needs no change to a mechanism two
  records argued for.

---

## Appended 2026-09-18, fifth section: the settled shape, after a conversation that moved every part of it

The four sections above were written while the proposal was still moving. This
is where it stopped. It supersedes §Proposal's part 3 and part 4 and renames the
table; parts 1 and 2 stand as written.

### What nothing here can do: delete

Established by reading rather than argued, and it reframes the whole table.
`self.turns` has no `remove`, no `drain`, no `clear`, no `retain`. **Nothing in
this system deletes a turn.** Both ratchets are indices into a vector that keeps
everything:

```rust
floor:  usize,   // do not send turns below this
pruned: usize,   // send those below this without their code
```

So `evict` and `prune` are words about destruction naming operations that only
choose what to send. That is the whole reason the table is not called `[window]`
and not called `[render]` either.

### The name: `[resend]`

`[render]` was this record's own suggestion and it over-claimed. The codebase
uses *render* for assembling the whole prompt — system block, tools, map,
history — and these three rules govern none of that. Every one of them acts only
on something **already sent at least once**: rule A on a span already visible in
the window, rules B and C on an old turn's content. `[resend]` names that set and
nothing else, and it names the waste the rules exist for: the prompt is rebuilt
whole every turn, so the entire history block is a resend.

### The keys, split by content and not by origin

The first split proposed here was `snippets` against `output`, and it was wrong
for a reason worth keeping: `read_file`'s result **is** a snippet. This project
already says so — *"an 8 KiB `cat` and a 1 000-token fragment are the same object
to the window"*. Today's code splits them anyway, because a selected span lives
in `Turn::code_context` and a tool result lives in a `ToolStep`. That is where
the bytes sit in the struct, not what they are.

| what reaches the prompt | how it got there | governed today by |
| --- | --- | --- |
| `sandbox.rs:40-95` | the selector chose it | rule B (`prune`) |
| `sandbox.rs`, whole | `read_file` returned it | rule C (`results`) |
| `cargo test` output | `run_command` returned it | rule C (`results`) |

The first two rows are the same thing under different rules; the last two are
different things under the same rule. Cutting by content instead:

```toml
[resend]
file-read  = "once"          # once | always
cmd-output = "always"        # once | always
when-full  = "drop-content"  # drop-content | drop-turns
```

- **`file-read`** — file bytes, however they arrived: selected by the selector or
  returned by `read_file`. `once` sends them in the oldest turn of the window
  that carries them and in no turn after; `always` gives every turn that asked
  its own copy.
- **`cmd-output`** — what a command produced. Same two values.
- **`when-full`** — the other question entirely, and the one nobody currently
  chooses: when the window does not fit, `drop-content` makes a turn give up its
  content and keep its exchange behind a citation, `drop-turns` drops whole turns
  with their question and answer. **Today's default is `drop-turns`, and it is a
  default nobody picked** — it is what happens because rules B and C ship off.

Two things this fixes about the earlier shape. The `once | always` the
conversation first proposed expresses rule A and cannot express B or C, because
*give it up under pressure* is not a number of times — and the two **compose**:
a span can be sent once and still be given up when the window fills. And `evict`
was rejected as a value because `Eviction` already exists as a separate setting
choosing `Turn` against `Block { low_water }`, under a comment saying it is *not
a preference*. Two settings spelled the same would be two questions spelled the
same.

Open: `write_file` and `edit_file` return confirmations, which are neither file
bytes nor command output and are too small to be worth a third key. They fall
under `cmd-output` by default and nobody has argued for that.

### Fragments across a resume: the reference, not the bytes

§Proposal did not cover this and the conversation found it. Today
`Context::from_view` rebuilds every turn with `code_context: Vec::new()` and
`pruned: 0`, because `SessionView` carries no fragments. That is the only place
in this system where something is genuinely lost rather than merely not sent.

Storing the bytes was rejected for a reason that is not the obvious one. The
obvious objection — *stored bytes go stale* — does not hold: `code_context` is
written once, at `push_turn_with_steps`, and **nothing ever refreshes it**, so a
live session already sends the bytes a turn saw even after a later turn edited
that file. A resume storing them would be no worse. What decided it is the
sandbox: bytes in the store come back **without being re-read**, so a session
resumed under a narrower posture gets back a file it is no longer allowed to
open. That is 2026-09-17's defect re-entering through the store.

So: **the reference, anchored in the session's timeline — the turn, which gives
the order, and the file it points at.** A digest was proposed here and dropped as
more than the question needs.

### Where `[resend]` applies, and where compaction does

A closed job never reaches these rules. `items_from` absorbs a closed job's
consecutive turns into one `Item::Folded` **before** the render sees them, and
that arm pushes the objective and the summary and reads neither `budget.repeat`,
nor `self.pruned`, nor `budget.results`. Its turns are not skipped by the rules;
they have stopped being turns.

And the fold is safe to prune against: `Job::close` builds the summary from the
job's steps and the fragments its turns were handed — *"All three are facts about
what happened; nothing the model said about what happened is used"* — so what a
turn stopped **sending** never changes what the job will summarize. Prune the
prompt, not the evidence.

Which leaves the shape the conversation settled on:

| | what shrinks it |
| --- | --- |
| a closed job | compaction, already done |
| an open job | `[resend]` |
| turns in no job | `[resend]` |

### The job lifecycle, and the one decision taken here

`Turn::job` is `Option<JobId>` and the third row above is why. A job is an
approved plan — `Proposed` says *"nothing runs in this state"* — so requiring one
per turn would put the gate in front of every question. The cost of the `Option`
is that **a turn in no job has no compaction path at all**: it can only be
pruned or dropped, never folded, and the largest saving in the system is
unavailable exactly where a conversation accumulates unchecked.

A lifecycle that closes that was sketched and is recorded here as a direction,
not as this proposal's scope:

```
job: draft      → turns (exploring; nothing runs under a plan)
  decision: approve | decline
job: planned    → turns (the live ones, sent verbatim)
  decision: complete | more changes
job: completed  → folded to objective + summary
```

It makes `job` non-optional, gives the exploratory turns somewhere to live, and
keeps the gate's meaning intact, because drafting is talking. Today's states map
onto it directly, reopen included — `Job::summary` is already documented as
dropped on a reopen.

**And the decision taken:** a draft does **not** fold while it is open. It was
the live question — a long draft has the same window problem a jobless turn has
today — and the answer chosen is that `[resend]` is what covers an open draft.
That makes the two mechanisms exhaustive and non-overlapping: **`[resend]` for
what is open, compaction for what is closed**, and no third case. It is a
decision on shape rather than on a measurement, and the thing that would
overturn it is a draft long enough that citations are not enough.

---

## Appended 2026-09-18, sixth section: two corrections to the section above it

Both are the author's, both against this record, and both are right. The fifth
section is wrong on them and stays where it is.

**There are no turns without a job.** §fifth's table has three rows and should
have two. Under the lifecycle agreed one message earlier, a turn is born inside
a `draft`, so `Turn::job` stops being `Option<JobId>` and *turns in no job* is
not a case. §fifth also recorded that lifecycle as "a direction, not as this
proposal's scope", which was hedging: it had been decided.

| | what shrinks it |
| --- | --- |
| a closed job | compaction, already done |
| an open job, draft or planned | `[resend]` |

**`when-full` does not belong in `[resend]` and was this record's invention.**
It does not govern resending. It governs what happens when the window does not
fit, which is the question `Eviction` already answers — and §fifth had itself
rejected the value `evict` for colliding with that setting, without noticing
that the collision was the key and not the value.

And the stance that replaces it is stronger than the knob: **do not add an
option, make it visible.** A window that no longer fits is a fact the person
should be told, not one the system absorbs quietly. The material is already on
the wire — `TurnView::dropped` carries the `Evicted`, and the debug panel reads
it — so what is missing is not the data but that nothing treats it as something
a person is *told*. Left here as the shape of the answer and not as a design:
what an error means for a surface that must still answer the turn is unargued.

So the table is the two keys it was first proposed as:

```toml
[resend]
file-read  = "once"    # once | always
cmd-output = "once"    # once | always
```

**What this costs, said plainly:** rules B and C have no key here. `[resend]`
now expresses rule A over two content types and nothing else. Whether pruning
under pressure stays a flag, joins `Eviction`, or disappears in favour of
telling somebody the window is full is open, and item 5 — whether a model minds
losing tool output to a citation — is unchanged by any of it.

**The pattern in these six sections, which is the honest summary of them.** Four
of the six corrections went the same way: this record borrowed a shape (the
posture's refusal), a word (`render`), a seam (where the bytes sit in the
struct) and a knob (`when-full`) from somewhere they already existed, and each
time the borrowed thing brought an assumption with it that nobody had checked
against what was being built. The corrections did not come from measurement.
They came from being asked what the thing actually does.

---

## Appended 2026-09-18, seventh section: the job lifecycle, settled

§fifth recorded the draft/planned/completed sketch and §sixth corrected the
hedge around it. This is the shape it ended at, and it retires a question §fifth
left open.

**Approving is closing.** A draft is a job, and approval is not a transition
inside it — it is the job **ending**. What follows is a second job, whose
objective is the approved plan.

```
job 1  draft     objective: what the user asked, as they asked it
                 turns: exploring
                 approve → close and fold, and open job 2

job 2  plan      objective: the approved plan
                 turns: the work
                 complete → close and fold
```

**One rule, symmetric on both ends: a job ends only on its affirmative
decision.** `decline` and `more changes` are the same answer — *not yet* — and
both keep you in the job you are in. Nothing closes, nothing folds, the
conversation continues.

What this buys, and the first is the one that matters:

- **The draft folds at approval**, so §fifth's decision — *a draft does not fold
  while it is open* — stops being a compromise and becomes simply true. `[resend]`
  covers a draft while it is being drafted; the fold takes it the moment somebody
  decides. The question of an open draft growing unbounded was the live objection
  to that decision, and approval answers it.
- **The draft's summary is the plan's context.** Exactly what a reader needs and
  in the right order: this is what we looked at, this is what we concluded, this
  is what we are now doing.
- **`Item::Folded` does not change.** It renders objective and summary and never
  asks why a job closed. `ClosedBy` is already the place that answers that, and
  already exists to make each rung *countable* — so `Approved` is a third variant
  with the same rationale the first two were given.
- **At most one job is open at a time**, and everything behind it is a fold. The
  window in steady state is *N small summaries plus the open job's turns*, which
  is the whole context model in one line.

**What moves rather than disappears.** `JobState::Rejected` is terminal today and
deliberately kept — *"that a plan was put up and turned down is worth more than a
gap in the numbering"*. Under this shape a decline ends nothing, so that fact
stops being a job state and becomes a **decision event inside the draft**. The
information survives the move and so does the reason for keeping it: how many
rounds a plan takes before it is approved is a number worth having, and a
lifecycle where decline is silent cannot produce it.

Still open, and not small: nothing here says what opens the first draft of a
session, or what opens the next one after a job completes. Both are presumably
*the next turn*, which would make a session a chain of drafts with plans between
them — but that is a sentence this record is writing to notice it is unwritten,
not one it is deciding.

---

## Appended 2026-09-18, eighth section: a session is an alternation, and that closes §seventh's open question

§seventh ended by naming what it had not written: what opens the first draft,
and what opens the next job after one completes. Both are answered by one rule.

**A session always opens with a `draft`, and jobs alternate.**

```
session:
  job: draft   compacted
  job: plan    compacted
  job: draft   compacted
  job: plan    live
```

`approve` closes a draft and opens a plan; `complete` closes a plan and opens a
draft. Strict, because the negative decisions keep you where you are — there is
no transition that produces two drafts in a row.

What it settles:

- **Every turn has a job from the session's first turn**, with no approval
  needed to reach one. The `Option<JobId>` that §sixth removed had exactly one
  job: holding the turns before a plan existed, and the opening draft holds them
  now.
- **The window reads as an alternation** — *what we discussed*, *what we did*,
  *what we discussed*, *what we are doing* — which is a better history than N
  summaries of one kind.
- **The job's kind is derivable from its position** and should still be stored:
  a fold that has to be counted to be understood is a fold a reader cannot
  check.

Two consequences, named because they are the kind that gets discovered instead:

- **A session that approves nothing is one draft that never closes**, so
  compaction is never available to it and `[resend]` is its only mechanism. That
  is consistent with §fifth's division rather than a hole in it, and it is the
  same position today's jobless turns are in — the difference is that this one
  has somewhere to fold to the moment anything is approved. It is also the
  ordinary shape of a session that only asks questions, which is most of them.
- **A zero-turn draft is reachable**: complete a plan, approve the next one
  immediately, and the draft between them closes with nothing in it. `Job::close`
  takes a turn count and accepts zero, so it would fold to an objective and an
  empty summary — two messages of noise in the window. Either a draft does not
  open until a turn lands in it, or a job with no turns does not fold. Small, and
  a decision rather than an implementation detail.

---

## Appended 2026-09-18, ninth section: part 1 built, and the field was worse off than §third said

Part 1 is in: `record::FORMAT` 10 → 11, three `Option<_>` fields on the header
beside `eviction`, written by `session::header` out of the `Budget` it was
already holding. The record stays **WIP** — parts 2 through 5 are unbuilt, and
this is one of five.

**What building it found, and it sharpens §third's sentence rather than
contradicting it.** §third said the three fields were *"in the same struct, in
its hand, unread"*, and read as an omission at the call site. It was more than
that: **`Repeat`, `Prune` and `Results` derived no `Serialize`**, and neither
does `Budget`. The fields were not unwritten, they were **unwritable** — no
amount of correcting `session::header` would have put them on a wire, because
there was no wire for them to go on. `Eviction`, in the same struct and one
field along, has derived it since format 2.

So the three rules are the only part of this window's policy that had **never
been serializable in any form** — not to a record, not to a session store, not
to the page. The `pruned` trace line carries turn numbers and token counts and
was the whole of what a reader had; it never carried a `Prune`. That is the same
defect §Problem part three named, one level lower than anybody had looked, and
it is why the diff touches `context.rs` at all — a part-1 diff that was only
about a header should not have needed to.

**Two things that came for free, both worth naming because neither was
planned.**

`retarget_header` gains all three without a line: it builds its line through
`session::header` with the destination's `Budget`, so a session that moves
mid-stream already records which rules the rest of it ran under. §third wanted
that mechanism for part 3's live changes and it is already sitting there, which
makes part 3 smaller than §third costed it.

And the page's `FORMAT` had to move with the host's, which is not free but is
*automatic*: `ui_versions.rs` fails the moment they disagree, so the constant in
`store.js` was updated by a test rather than by remembering. That test exists
because the two drifted when `FORMAT` went to 8 and the UI could not open a
session at all. Second format bump since, second time it did its job.

**What is pinned.** Three tests, and the middle one is the one that would catch a
regression rather than a typo:

- a round-trip per rule, both arms, asserting that the two arms do **not** read
  back the same — a header that cannot tell two runs apart is the one thing this
  format exists to prevent;
- `session::header` writes every rule the budget holds, asserted on both arms,
  because a function that hard-coded `Once` passes a one-arm test;
- a format-10 header, keys absent, reads back `None` on all three — the claim the
  record never made stays unmade.

**Still open, unchanged by this.** Parts 2 through 5, the default flip (part 1
records the arm, it does not choose it), item 5, and everything §fifth through
§eighth settled about `[resend]`, the job lifecycle and the alternation. Nothing
here decides any of them; it makes the stream able to say which one was in force,
which is what they were all waiting on.

---

## Appended 2026-09-18, tenth section: part 2 built, and it dragged part 3's blocker in with it

Part 2 is in — *per session, not per process*, which §Proposal calls the real
work. The record stays **WIP**: parts 3, 4 and 5 are unbuilt, and the `[resend]`
shape §fifth through §eighth settled is a change to what a rule is *about*,
which none of this touches.

**What landed.** `POST /api/sessions` and the resume route take `repeat`, `prune`
and `results` beside `provider`, `model` and `posture`, each absent meaning
*whatever is in place*. They are named by the header's own words — `always`/`once`,
`never`/`behind`, `kept`/`cited` — so the request that started a session and the
recording of it say the arm the same way, and a value outside the enum is a 400
for the reason a provider is a name out of a file and never a URL. The budget
they produce rides in the `Destination`, which is what a turn reads, so the
choice reaches `select` rather than only the header. `/api/settings` reports all
three, because a fact a session may choose and nothing can read back is a fact
the page can contradict — §Proposal part 4's argument, one surface earlier than
the modal.

**A session that names none keeps what is in place**, and this is a decision
rather than a fallback. It is the *destination's* rule and not the posture's: a
posture is re-resolved every session because nothing may inherit a container,
and these grant nothing — the same asymmetry §third used to withdraw the refusal
on a resume. A new session already keeps the last one's model; the rules live in
the same struct and now behave the same way.

### The thing this was going to leave behind, and could not

Making the rules movable on a resume re-created **the posture's own bug, one
field along.** `retarget_header` compared backend, model and posture; a resume
that changed only the rules moved nothing it compared, so it wrote no line, and
the rest of the stream read as though it had stayed under the old ones. That is
[`what-an-approval-was-granted-under`](2026-09-17.what-an-approval-was-granted-under.completed.md)'s
sentence with three different fields in it, and it would have been true the
moment this route shipped.

So the fourth term is in, and with it the half §second said part 3 was blocked
on: `SessionView` carries the three as `Option<_>`, folded out of the stream's
last header exactly as `posture` is, `None` meaning *a recording from before
format 11 that does not say*. A fold that cannot say what it ran under is not
*the same as* the defaults, so it gets the line — the same answer the unknown
posture gets, for the same reason.

**Which makes part 3 smaller than §third costed it.** §third wanted a mechanism
for changing the rules mid-session and named `retarget_header` as the one that
already existed; what it did not know is that the mechanism had to *learn the
rules first*, and that learning them is most of what part 3 needed the fold for.
What remains of part 3 is the `[window]`/`[resend]` table, the `PUT` beside the
providers one, and its two guards.

### Found while checking, and left where it was found

A `serve --record` file holds **one header for the whole process.**
`create_session` and `resume_session` write their header into the *store's*
stream; the recorder takes `Event`s, which are protocol and trace lines only,
and has no way to be handed a header. So a `--record` file spanning two sessions
names the first one's model, posture and rules for all of it.

This predates the rules by every field it already carried — the posture has been
in that hole since format 10 — and it is **not the two-line fix it looks like.**
`Recorder::write` computes `at_ms` against the *process's* start and
`App::publish` computes the store's against the *session's*. A header declares
the base every later line is relative to, so writing a second one into that file
is a decision about re-basing it, not a line to add. Recorded as its own row
rather than folded into this diff, which is what §Problem part three did on
2026-09-18 and the reason part 1 was a good part rather than a good paragraph.

### Still open, unchanged

Parts 3, 4 and 5. The default flip, which part 2 makes *reachable* and does not
make. Item 5. And the whole of what §fifth through §eighth settled about
`[resend]`'s two keys, the job lifecycle and the alternation — none of which this
touches, because who chooses a rule and what a rule is about are different
questions and this one answered the first.

---

## Appended 2026-09-18, eleventh section: part 3 built, with the keys the record argued against

Part 3 is in: a `[resend]` table in `config.toml`, `GET`/`PUT /api/resend` under
the providers route's two guards, and the live session moved onto what is safe
to move it onto. The record stays **WIP** — parts 4 and 5 are unbuilt.

### The keys are today's names, and that is against §sixth

**Said first because it is the part that contradicts a decision already
recorded**, which this project's own rules say is to be argued with rather than
routed around.

§sixth settled two keys cut by content — `file-read` and `cmd-output` — and it is
the better cut. It is also a cut **nothing implements**: `file-read` means file
bytes however they arrived, and rule A reaches selected spans only; `read_file`'s
result is reachable today just like `cargo test`'s, through rule C, behind rule
B's line. So the settled table describes a capability that does not exist, and
writing it now would name a file after behaviour the code cannot perform. The
table ships under `repeat`, `prune` and `results`: the header's own words, which
part 1 put on the wire and part 2 put in the session request, so a machine's
table, a session's ask and a recording all say the arm the same way.

**What it costs, plainly: renaming them later is a migration of a file on
somebody's machine**, and `config.toml` is the one file in this project a person
owns. The alternative was building the content cut first — a render change and a
decision about what `cmd-output = "once"` even means, which §fifth left as one
line — and holding the mechanism until then. Chosen knowingly, by the person
whose machine it is, with the cost above on the table. It is recorded here in
the shape this file uses for a decision that goes against an earlier one:
**§sixth is not withdrawn.** It is still the cut this table should have, and
the rename is now owed.

### §fourth, as code

What the route **writes** and what it **applies** are not the same set, which is
the fourth section's asymmetry in the one place it was always going to matter.
The file always takes the write. The live session takes only what is free:

- **Rule A** moves both ways: it stores nothing, its owner is chosen inside the
  window being rendered.
- **Rule B off** does not move a running session. The policy gates the decision
  and the line gates the render, and they are not the same gate, so turning it
  off would advance nothing and return nothing — a control reporting a change it
  did not make.
- **Rule C off** is worse than that, and this is why the route does not simply
  warn. Handing tool output back makes the prompt bigger, what absorbs bigger is
  the floor, and the floor takes the question and the answer with it and does not
  come back.

Each withheld rule comes back in a `waiting` line saying what the file now says
and what this session keeps doing. §fourth called *live on, session-start only
for off* the honest option and declined to decide; this is that option, decided.

### The thing that would have shipped as a lie

`put_resend`'s withheld rules promise *until the next one starts*. Part 2 had
decided that a session naming no rule **keeps what is in place** — so the next
session would have inherited the running one's rules, the write would never have
reached a session at all, and the only way to act on it would have been
restarting the binary. The message would have been false in exactly the case it
exists for.

So part 2's rule gains a middle level, and this is a **correction to a decision
three commits old**: *what the session asked for*, else *what `[resend]` says*,
else *what is already running*. The file in the middle is the table's own job —
§Proposal's comment on it reads *the machine's default, and what a session starts
on unless it says otherwise* — and where the file states no position, part 2's
behaviour is unchanged, because a machine that says nothing is not a machine
saying the default.

Found by reading the message the route had just been given and asking whether it
was true. Sixth time in twelve days that the defect was in the thing nothing
drove, and the first where what nothing drove was a sentence this session had
written ten minutes earlier.

### Two smaller decisions, both stated because neither is obvious

**The table is re-read at every session start, not held from startup.** That is
the posture's precedent — `posture_for` re-resolves the policy file so a new
session picks up an edited `luu.toml` — and it applies with more force here,
because this is a file the page itself writes: a table saved at turn 12 that
only reached sessions started before it would be a control that does nothing.
It is deliberately the opposite of the rule the postures *list* follows, where
what a surface offers must not change under it.

**`chat` and `stdio` read it too.** §Still open asked whether `chat` should
follow `serve` and left it as an unargued seam. Half of it closes here: a table
in the file that already holds this machine's default destination is a fact about
the machine, and every surface reads it. What stays open is the other half — the
CLI has no session-start moment, so nothing there can choose per session.

And the precedence is **one-way**, which the flags' own shape decides: a `bool`
flag can only turn a rule on, so *absent* and *off* arrive as the same `false`.
`--repeat-once` beats a file that says `always`; a file that says `once` cannot
be turned off from the command line. That is the `--no-repeat-once` §What it
costs predicted, still absent because no default has flipped, and pinned by a
test as what the code does rather than left to be met by surprise.

### Still open

Parts 4 and 5. The key rename owed to §sixth. The default flip, which all three
parts so far have made *reachable* and none has made. Item 5. And item 23's
recorder, untouched: a `serve --record` file still holds one header for the whole
process, so the second header this part writes into the store's stream — the one
that says the rest of this session was rendered under something else — does not
reach that file.

---

## Appended 2026-09-18, twelfth section: part 4 built, and the modal's own claim turned out to be half true

Part 4 is in: a third Settings section, *Resend*, beside Models. The record
stays **WIP** — part 5 is unbuilt, and so is the key rename §eleventh owes
§sixth.

**The section is where §Proposal said it was, on the argument it gave.** General
holds Theme, Editor, Layout, Files and Workspace — five facts about the screen,
in `localStorage`, where one server answers two people on two machines and
neither's preference changes what is sent. These three change the bytes the model
receives. A preference that changes the prompt is not a preference, so the
section sits beside Models, which is the other thing in that modal that is about
the run rather than about the reader.

**What it shows, and one decision inside it.** Two panels: what the live session
is rendering under, and this machine's `[resend]` table below it, with the rows
where they disagree marked — because a session started under a rule nobody wrote
down is the ordinary case, not an error. Each rule carries what it *bought*:
rule A's 17.5% with no verdict moved, rule B's 0 turns evicted against 13, rule
C's 32 752 tokens — **and that two of the three have no accuracy measurement at
all**, with rule C's row saying outright that tool output is evidence and that
the corpus its number comes from pruned 20 `read_file` calls and no command
output. That is not hedging in a UI. This panel is the first place a person is
asked to turn one of these on, and a control that shows a saving and hides that
nothing has measured its cost is a control that makes the decision for them.

`unset` is its own position beside the two values, for the same reason `None` is
its own value in the header and in the table: a machine stating no position is
not a machine choosing the default.

And the `waiting` lines are **reported, never predicted**. The server decides
which rules a running session was not moved onto; the page prints what it is
handed. A browser working that out from the two ratchets would be a second copy
of a rule that already has one, in the language least able to check it — the same
sentence this project has written about `is_this_machine` and about
`unconfigured`.

### The modal said a third section was free. It was not, quite.

`settings-modal.html` has carried this comment since the three-column record:

> *The rail. Two sections today, and the shape is what makes a third free: a
> section is a name and a component, not a place in a scroll.*

The shape held — the rail took a name and a component and nothing else moved. But
adding the third section **stopped the settings modal opening at all**, and it
did it silently.

`asModal` is called from the modal's `:setup` and schedules one
`requestAnimationFrame`; if the element is not in the document yet it returns,
without a word. jq79 runs `:setup` to completion before rendering the template,
and the call is followed by one `await import` per section. **Two sections
resolved inside the frame. Three did not.** A `<dialog>` that was never shown is
in the document and invisible, so there was no error anywhere: the button was
clicked, the component mounted, the rail rendered, and nothing appeared.

So `asModal` waits *across* frames now, up to a bounded number, which fixes the
class rather than this instance — the race was always there and the number of
sections was only what decided it.

**How it was found is the part worth keeping.** By a browser test clicking the
new section, written because
[`a-test-that-clicks-approve`](2026-09-08.a-test-that-clicks-approve.completed.md)
exists for exactly this: *two bugs in two days were found by a person opening
this page and none by a test.* Nothing in the Rust suite could have seen this,
and nothing in the static twin either — it is a race between a component's setup
and its own render. Seventh time in twelve days that the defect was in the thing
nothing drove, and the first one that was in the mechanism a record had already
declared safe to extend.

The new spec is `tests/smoke/settings.spec.js`, beside the gate's, with its own
server, port and state directory — it *writes* `config.toml`, so running it
against the config of the person running it would edit their machine. It drives
the whole of part 3 from the browser: rule A on and written to the file beside
the provider it must not delete, rule B on, and then rule B off — where it
asserts that the running session keeps pruning, that the file says `never`, and
that the page says both.

### Still open

Part 5. The key rename owed to §sixth. The default flip, which four parts have
now made reachable and none has made — **and the page is what changes that
question**: until today the argument for flipping a default was partly that a
flag nobody can reach is a feature nobody has, and that half of it is now
answered by three buttons. What is left of it is item 5 and n=1, which is where
it should have been all along. Item 23's recorder is still untouched.
