To read the file `crates/agent-core/src/backend/mock.rs`, I'll use the `read_file` tool:

```tool
{"name": "read_file", "arguments": {"path": "crates/agent-core/src/backend/mock.rs"}}
```

  [1] → read_file {"path":"crates/agent-core/src/backend/mock.rs"}
  [1] ← ok · . (read-write) · held by in-process check · 0 ms

The `Mock::cycle` method in the `mock.rs` file controls the behavior of the mock backend when it runs out of replies. Here's what it does:

1. **Default Behavior**: When `cycle` is `false` (the default), the mock backend will return the last reply it has once it runs out of replies. This means that if you have a list of replies like `["call", "answer"]`, the backend will return "answer" indefinitely after the second call.

2. **Cycling Behavior**: When `cycle` is `true`, the mock backend will wrap back to the first reply in the list once it runs out of replies. This means that if you have a list of replies like `["call", "answer"]`, the backend will cycle through "call" and "answer" repeatedly.

The method takes a boolean parameter `cycling` and sets the `cycling` field of the `Mock` struct to this value. It then returns the modified `Mock` instance.

Here's the relevant part of the code:

```rust
/// Off by default, so every existing script — one reply that repeats, or
/// a call-then-answer pair that answers forever after the first tool
/// loop — keeps meaning what it always meant. On, the list wraps back to
/// its first element instead of sticking on its last, which is how a
/// `call, answer` pair becomes a tool invoked on every turn of a script
/// of any length, rather than once at the start of the session.
pub fn cycle(mut self, cycling: bool) -> Self {
    self.cycling = cycling;
    self
}
```

This method allows you to control whether the mock backend should cycle through its replies or repeat the last reply once it runs out.

[stop] 2548 prompt / 424 completion
