# The fix against its parent commit, 2026-09-20

The run behind
[`the-newest-body-wins`](../../2026-09-20.the-newest-body-wins.completed.md).
Same corpus, same replies and same flags as
[`2026-09-19.edit-reread-mock`](../2026-09-19.edit-reread-mock/) and
[`2026-09-19.one-counting-surface`](../2026-09-19.one-counting-surface/) — the
third time this corpus has been run and the first time the **binary** is what
moves. The repair is not a flag, so *one flag apart* is one commit apart here:
`before` is `2e87d2c`, `after` is the commit this directory belongs to.

Mock backend. The number is a fact about the corpus, not about a model.

## What the tally says

|  | diverged | superseded | repeated |
| --- | --- | --- | --- |
| `before` | 8 renders, 2 asking, 1 path at 3 bodies | — (no such line) | 7 renders, 7 spans |
| `after` | **0** | **8 renders, 10 spans, 898 tokens** | 7 renders, 7 spans |
| `always` (`--no-repeat-once`) | 8 renders, 2 asking, 1 path at 3 bodies | 0 | 0 |

`before` and `always` agree on the divergence and disagree on everything else,
which is the pair worth reading: the fix does not make the contradiction go away
by making the corpus quieter, it replaces the stale body. Rule A off is where the
old numbers live now, and they are the same numbers.

**Ten spans over eight renders.** After the second edit a single prompt carries
two stale bodies — turn 1's and turn 6's — and replaces both. A divergence is a
state the window is in; so is its repair.

## What it cost

[`prefix-reuse.md`](prefix-reuse.md), generated by
[`prefix.py`](prefix.py) from the recordings' own `prefix_reuse` lines, because
`luu count` has no opinion about prefixes and should not grow one: a prefix is a
fact about two consecutive renders and every number that surface carries is a
fact about one session.

Session prefix reuse **93.95% → 87.14%**, and all of it in turns 6 and 12 — the
two turns that re-read the edited file. The history bucket fell **7 200 → 6 432
tokens**.

## Running it again

```sh
git worktree add /tmp/luu-before 2e87d2c
(cd /tmp/luu-before && cargo build --bin luu)
cargo build --bin luu
BEFORE=/tmp/luu-before/target/debug/luu sh RECORD/runs/2026-09-20.the-newest-body-wins/run.sh
```

`.jsonl` recordings are gitignored project-wide and `run.sh` deletes them after
`prefix.py` has read them; the `.txt` and `.md` files here are the committed
evidence.
