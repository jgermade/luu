# The drift was never the backend

Closes the thread
[`a-grammar-for-tool-calls`](2026-09-06.a-grammar-for-tool-calls.completed.md) opened
when the 7B's `Drifted` rate on `llama-server` (6/15) came back three times
higher than the same corpus's original result on Ollama (2/15,
[`the-tool-call-probe-run`](2026-09-14.the-tool-call-probe-run.completed.md)),
flagged there as "almost certainly the backend... but this record has no way
to separate 'different backend' from 'different day.'"

It was not the backend. It was which `qwen2.5-coder:7b` — the file, not the
server — and the install that made the comparison possible arrived mid-session
(the person running this repository installed `ollama`/`ollama-cuda` after
being asked, so machine 4 has both engines for the first time).

## The triangulation, three runs

All three: the official fifteen prompts, `--context-limit 8192 --temperature
0 --seed 7`, machine 4, scored by the real `score_call`.

1. **[`runs/2026-09-15.7b-argv-schema/`](runs/2026-09-15.7b-argv-schema/)** —
   the raw HuggingFace GGUF this machine already had
   (`qwen2.5-coder-7b-instruct-q4_k_m.gguf`) over `llama-server`. 6/15
   Parsed, 6/15 Drifted.
2. **[`runs/2026-09-15.7b-ollama-same-gguf/`](runs/2026-09-15.7b-ollama-same-gguf/)** —
   the identical file, imported into Ollama with a bare `FROM <path>`
   Modelfile (no custom `TEMPLATE`, so Ollama reads the GGUF's own embedded
   Jinja `chat_template` — confirmed byte-identical to what `llama-server`
   reports at `/props` for the same file: same file, same template, either
   engine renders it the same). Every one of the fifteen replies came back
   **byte-identical** to run 1, verdict for verdict, character for
   character. The serving stack is not a variable here at all.
3. **[`runs/2026-09-15.7b-ollama-registry/`](runs/2026-09-15.7b-ollama-registry/)** —
   `ollama pull qwen2.5-coder:7b`, the exact model name
   [`the-tool-call-probe-run`](2026-09-14.the-tool-call-probe-run.completed.md)
   used on machine 1. **3/15 Parsed** — the original discovery's 20%,
   reproduced almost exactly, on different hardware, a different Ollama
   version, and eleven days later. The per-prompt breakdown is not
   identical (4 Drifted / 3 ContinuedPastFence / 5 NoCall here against 2 /
   2 / 8 there — different days, different machines, both true), but the
   headline number both runs converge on is the same one.

## Why: two files with the same name

`sha256sum` on the local GGUF: `509287f7...`. `ollama show qwen2.5-coder:7b
--modelfile`'s `FROM` line points at a blob hashed `60e05f21...`. Different
bytes, both labeled Q4_K_M, both 4.7 GB, both "qwen2.5-coder:7b" to a human
— Ollama's registry build and the upstream HuggingFace conversion are not
the same file. Likely a different quantization calibration (an importance
matrix built against a different corpus, or a different `llama.cpp` version
at conversion time) rather than a different nominal quant level, since the
label and size match.

**The template is not why, even though it also differs.** `ollama show
qwen2.5-coder:7b --template` is Ollama's own hand-authored Go template, not
the GGUF's embedded Jinja — a second, real difference sitting right next to
the weights one, easy to blame instead. But this project never sends the
API's native `tools` field
([`openai.rs`](../crates/agent-core/src/backend/openai.rs) has no `tools:`
in its request body, confirmed by grep before writing this), so both
templates' `{{if .Tools}}`/`{%- if tools %}` branches are dead code on every
call `luu` makes. Read against what's left — a plain system message and a
plain user message — the two templates wrap them the same way:
`<|im_start|>{role}\n{content}<|im_end|>\n`, structurally equivalent between
Jinja and Go here. Run 2 above is the direct proof: same weights, two
different template-rendering engines, byte-identical output. Whatever a
different template *could* do to this corpus, it did not do it in the one
controlled comparison this record has.

## What this means for the fix being checked

**No envelope collapse in any of the three runs — 9 `run_command` attempts
across them (4, 9, 14 in each), all correctly shaped `{"argv": [...]}`.**
The 2026-09-15 `argv` schema change was never in question here; this thread
was about a different, adjacent number (the `Drifted` rate) that looked like
it might implicate the backend work depends on. It does not. Machine 4's
`llama-server` and Ollama agree with each other on the file this repository
actually has; the number that looked anomalous belongs to a file this
repository does not have and was only ever compared against by name.

## Still open

- **Which specific difference in the registry build causes the gap** —
  quantization calibration, `llama.cpp` version, something else in Ollama's
  conversion pipeline — is not identified, only located to "the weights,
  not the serving stack or the template." Answering it further would need
  the two GGUF files compared directly (tensor-level diff, or re-running
  Ollama's own conversion locally), which this record does not attempt.
- **Whether the 14B has the same two-file problem** is untested — every
  14B run in this project's history has used the same local HuggingFace
  GGUF; no `ollama pull qwen2.5-coder:14b` comparison exists.
- `ollama-cuda` was installed alongside `ollama`; nothing here checked
  whether Ollama used the GPU for any of these runs (no per-run timing was
  captured, matching this project's own "scores answers, not seconds"
  stance) — mentioned only because a CPU-bound run would be a further
  confound this record did not have to rule out and got lucky on.
